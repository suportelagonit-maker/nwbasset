# Frontend Mobile NWB Asset (Expo)

Target folder: `frontend-mobile/`

## Comandos basicos

No diretorio `frontend-mobile/`:

```powershell
npm install
npm run start
```

## Dependencia

O app mobile consome a API do backend Laravel em `backend/`.
